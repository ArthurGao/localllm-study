# 01_basics 学习总结

## LLM 采样参数速查

### 核心参数

| 参数 | 作用 | 范围 | 默认 | Ollama | Groq |
|------|------|------|------|--------|------|
| `temperature` | 控制随机性。越低越确定，越高越发散 | 0.0 - 2.0 | 0.7 | ✅ | ✅ |
| `top_p` | 核采样。只从累计概率前 p 的 token 中选 | 0.0 - 1.0 | 0.9 | ✅ | ✅ |
| `top_k` | 只从概率最高的 k 个 token 中选 | 1 - ∞ | 40 | ✅ | ❌ |
| `max_tokens` | 最大生成 token 数 | 1 - 模型上限 | 模型默认 | ✅ (`num_predict`) | ✅ |
| `seed` | 固定随机种子，用于复现 | 任意整数 | 随机 | ✅ 确定性 | ⚠️ 尽力而为 |
| `num_ctx` | 上下文窗口大小 | 1 - 模型上限 | 2048 | ✅ 可调 | ❌ 模型固定 |
| `repeat_penalty` | 惩罚重复 token，减少复读 | 0.0 - 2.0 | 1.1 | ✅ | ❌ |

### 参数如何协作

```
输入 token → 模型输出概率分布
                ↓
          ① top_k 截断：只保留概率最高的 k 个候选
                ↓
          ② top_p 截断：在剩余候选中，按概率排序累加，保留到累计 ≥ p
                ↓
          ③ repeat_penalty：对已出现的 token 降低概率
                ↓
          ④ temperature 缩放：对剩余候选重新分配概率
              - 低温 → 高概率 token 更突出 → 输出更确定
              - 高温 → 概率更均匀 → 输出更随机
                ↓
          ⑤ 采样：按最终概率随机选一个 token（seed 控制这步的随机数）
                ↓
          ⑥ 重复直到遇到 EOS 或达到 max_tokens
```

### 常用组合

| 场景 | temperature | top_p | top_k | repeat_penalty |
|------|-------------|-------|-------|----------------|
| 精确问答 / 代码 | 0.1 - 0.3 | 0.9 | 40 | 1.0 |
| 日常对话 | 0.7 | 0.9 | 40 | 1.1 |
| 创意写作 | 1.0 - 1.5 | 0.95 | 100 | 1.2 |
| 完全确定性 | 0.0 | 1.0 | 1 | 1.0 |

### 关键理解

1. **temperature 和 top_p 通常二选一调节**，同时调容易互相干扰
2. **top_k 是硬截断，top_p 是软截断**——top_k 不管概率分布，top_p 会自适应
3. **seed 在云端不可靠**——分布式推理节点不同，结果可能不同
4. **num_ctx 影响能处理的文本长度**——太小会截断上下文，太大会消耗更多内存
5. **repeat_penalty > 1.0 减少重复，< 1.0 反而鼓励重复**——通常设 1.0-1.3
